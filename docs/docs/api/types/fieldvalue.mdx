---
title: FieldValue
sidebar_label: FieldValue
---

# FieldValue

A mapping between a schema type and its native variable type.

## Definition

**Type:** <code>FieldValue = T extends any ? string : T extends any ? number : T extends any ? boolean : T extends any ? Date : T extends any ? Set&lt;[CoreValue](/api/types/corevalue)&gt; : T extends any ? Dictionary&lt;string, [CoreValue](/api/types/corevalue)&gt; : T extends any ? [RichText](/api/interfaces/richtext) : [CoreValue](/api/types/corevalue)</code>

