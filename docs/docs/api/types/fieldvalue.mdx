---
title: FieldValue
sidebar_label: FieldValue
---

# FieldValue

A mapping between a schema type and its native variable type.

## Definition

**Type:** <code>FieldValue = T extends 'string' ? string : T extends 'number' ? number : T extends 'boolean' ? boolean : T extends 'date' ? Date : T extends 'set' ? Set&lt;[CoreValue](/docs/api/types/corevalue)&gt; : T extends 'map' ? Dictionary&lt;string, [CoreValue](/docs/api/types/corevalue)&gt; : T extends 'richtext' ? RichText : [CoreValue](/docs/api/types/corevalue)</code>

