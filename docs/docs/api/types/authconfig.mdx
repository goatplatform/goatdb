---
title: AuthConfig
sidebar_label: AuthConfig
---

# AuthConfig

An array of authentication rules for the full DB. The DB scans these rules
and will use the first one that matches the repository's path.

## Definition

**Type:** <code>AuthConfig = &#123; rule: [AuthRule](/docs/api/types/authrule); rulePath: RegExp | string &#125;[]</code>

