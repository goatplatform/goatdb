---
title: BloomFilter
sidebar_label: BloomFilter
---

# BloomFilter

A simple Bloom Filter implementation using MurmurHash3.

**Implements:** [Encodable](/api/interfaces/encodable), [Decodable](/api/interfaces/decodable)

## Constructor

**<code>new BloomFilter(options: [BloomFilterOptions](/api/interfaces/bloomfilteroptions) | [BloomFilterDecoderConfig](/api/interfaces/bloomfilterdecoderconfig)&lt;&gt;)</code>**

## Methods

### add()

**<code>add(values: string | Iterable&lt;string, any, any&gt;): void</code>**

Adds a value to the filter

### clear()

**<code>clear(): [BloomFilter](/api/classes/bloomfilter)&lt;&gt;</code>**

### deserialize()

**<code>deserialize(decoder: [Decoder](/api/interfaces/decoder)&lt;string, [DecodedValue](/api/types/decodedvalue)&gt;, _options: unknown): void</code>**

### fillRate()

**<code>fillRate(): number</code>**

### has()

**<code>has(value: string): boolean</code>**

Returns whether the key exists or not in the filter.

### reuse()

**<code>reuse(): void</code>**

### serialize()

**<code>serialize(encoder: [Encoder](/api/interfaces/encoder)&lt;string, [CoreValue](/api/types/corevalue), unknown, unknown&gt;, _options: unknown): void</code>**

