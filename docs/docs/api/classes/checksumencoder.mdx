---
title: ChecksumEncoder
sidebar_label: ChecksumEncoder
---

# ChecksumEncoder

**Extends:** [BaseEncoder](/api/classes/baseencoder)

**Subclasses:** [MD5Checksum](/api/classes/md5checksum), [Murmur3Checksum](/api/classes/murmur3checksum)

## Constructor

**<code>new ChecksumEncoder(opts: OT)</code>**

## Methods

### appendValue()

**<code>appendValue(state: ST, value: string, options: OT): ST</code>**

### checksumForString()

**<code>checksumForString(value: string, options: OT): string</code>**

### checksumForValue()

**<code>checksumForValue(value: object | [CoreValue](/api/types/corevalue), options: OT): string</code>**

### convertArray()

**<code>convertArray(arr: [ReadonlyCoreArray](/api/types/readonlycorearray), options: OT): string</code>**

### convertBoolean()

**<code>convertBoolean(b: boolean, _options: OT): string</code>**

### convertDate()

**<code>convertDate(date: Date, _options: OT): string</code>**

### convertDictionary()

**<code>convertDictionary(value: [CoreDictionary](/api/types/coredictionary), _options: OT): string</code>**

### convertEncodable()

**<code>convertEncodable(value: [Encodable](/api/interfaces/encodable)&lt;string, [CoreValue](/api/types/corevalue)&gt;, options: OT): string</code>**

### convertGenerator()

**<code>convertGenerator(g: Generator&lt;[CoreValue](/api/types/corevalue)&gt;, options: OT): string</code>**

### convertNull()

**<code>convertNull(_options: OT): string</code>**

### convertNumber()

**<code>convertNumber(n: number, _options: OT): string</code>**

### convertObject()

**<code>convertObject(obj: [ReadonlyCoreObject](/api/types/readonlycoreobject), options: OT): string</code>**

### convertSet()

**<code>convertSet(set: Set&lt;[CoreValue](/api/types/corevalue)&gt;, options: OT): string</code>**

### convertString()

**<code>convertString(str: string, _options: OT): string</code>**

### convertUndefined()

**<code>convertUndefined(_options: OT): string</code>**

### convertValue()

**<code>convertValue(value: object | [CoreValue](/api/types/corevalue), options: OT): string</code>**

### finalizeState()

**<code>finalizeState(state: ST, options: OT): string</code>**

### getDefaultOpts()

**<code>getDefaultOpts(): OT</code>**

### getOutput()

**<code>getOutput(): string</code>**

### isNativeValue()

**<code>isNativeValue(value: [CoreValue](/api/types/corevalue), options: OT): boolean</code>**

### newEncoder()

**<code>newEncoder(): [ChecksumEncoder](/api/classes/checksumencoder)&lt;ST, OT&gt;</code>**

### newState()

**<code>newState(options: OT): ST</code>**

### setPrimitive()

**<code>setPrimitive(key: string, value: string, _options: OT): void</code>**

## Inherited Methods

### From [BaseEncoder](/api/classes/baseencoder)

`convertKey()`, `set()`

*See [BaseEncoder](/api/classes/baseencoder) for detailed documentation*

