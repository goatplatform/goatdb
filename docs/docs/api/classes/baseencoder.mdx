---
title: BaseEncoder
sidebar_label: BaseEncoder
---

# BaseEncoder

**Implements:** [Encoder](/api/interfaces/encoder)

**Subclasses:** [ChecksumEncoder](/api/classes/checksumencoder), [JSONBaseEncoder](/api/classes/jsonbaseencoder)

## Constructor

**<code>new BaseEncoder(opts: OT)</code>**

## Methods

### convertArray()

**<code>convertArray(arr: [ReadonlyCoreArray](/api/types/readonlycorearray), options: OT): PT</code>**

### convertBoolean()

**<code>convertBoolean(b: boolean, options: OT): PT</code>**

### convertDate()

**<code>convertDate(date: Date, options: OT): PT</code>**

### convertDictionary()

**<code>convertDictionary(value: Dictionary&lt;string, [CoreValue](/api/types/corevalue)&gt;, options: OT): PT</code>**

### convertEncodable()

**<code>convertEncodable(value: [Encodable](/api/interfaces/encodable)&lt;string, [CoreValue](/api/types/corevalue)&gt;, options: OT): PT</code>**

### convertGenerator()

**<code>convertGenerator(g: Generator&lt;[CoreValue](/api/types/corevalue)&gt;, options: OT): PT</code>**

### convertKey()

**<code>convertKey(key: string): string</code>**

### convertNull()

**<code>convertNull(options: OT): PT</code>**

### convertNumber()

**<code>convertNumber(n: number, options: OT): PT</code>**

### convertObject()

**<code>convertObject(obj: [ReadonlyCoreObject](/api/types/readonlycoreobject), options: OT): PT</code>**

### convertSet()

**<code>convertSet(set: Set&lt;[CoreValue](/api/types/corevalue)&gt;, options: OT): PT</code>**

### convertString()

**<code>convertString(str: string, options: OT): PT</code>**

### convertUndefined()

**<code>convertUndefined(options: OT): PT</code>**

### convertValue()

**<code>convertValue(value: [CoreValue](/api/types/corevalue), options: OT): PT</code>**

### getOutput()

**<code>getOutput(): T</code>**

### isNativeValue()

**<code>isNativeValue(value: [CoreValue](/api/types/corevalue), options: OT): boolean</code>**

### newEncoder()

**<code>newEncoder(): [BaseEncoder](/api/classes/baseencoder)&lt;PT, T, OT&gt;</code>**

### set()

**<code>set(key: string, value: [CoreValue](/api/types/corevalue), options: OT): void</code>**

### setPrimitive()

**<code>setPrimitive(key: string, value: PT, options: OT): void</code>**

