---
title: MD5Checksum
sidebar_label: MD5Checksum
---

# MD5Checksum

**Extends:** [ChecksumEncoder](/api/classes/checksumencoder)

## Methods

### appendValue()

**<code>appendValue(state: MD5State, value: string): MD5State</code>**

### finalizeState()

**<code>finalizeState(state: MD5State): string</code>**

### newState()

**<code>newState(): MD5State</code>**

## Inherited Methods

### From [ChecksumEncoder](/api/classes/checksumencoder)

`checksumForString()`, `checksumForValue()`, `convertArray()`, `convertBoolean()`, `convertDate()`, `convertDictionary()`, `convertEncodable()`, `convertGenerator()`, `convertKey()`, `convertNull()`, `convertNumber()`, `convertObject()`, `convertSet()`, `convertString()`, `convertUndefined()`, `convertValue()`, `getDefaultOpts()`, `getOutput()`, `isNativeValue()`, `newEncoder()`, `set()`, `setPrimitive()`

*See [ChecksumEncoder](/api/classes/checksumencoder) for detailed documentation*

