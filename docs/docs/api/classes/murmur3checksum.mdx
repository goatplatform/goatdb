---
title: Murmur3Checksum
sidebar_label: Murmur3Checksum
---

# Murmur3Checksum

**Extends:** [ChecksumEncoder](/api/classes/checksumencoder)

## Constructor

**<code>new Murmur3Checksum(options: [Murmur3Opts](/api/interfaces/murmur3opts))</code>**

## Methods

### appendValue()

**<code>appendValue(state: MurmurHash3&lt;&gt;, value: string, _options: [Murmur3Opts](/api/interfaces/murmur3opts)): MurmurHash3&lt;&gt;</code>**

### finalizeState()

**<code>finalizeState(state: MurmurHash3&lt;&gt;, _options: [Murmur3Opts](/api/interfaces/murmur3opts)): string</code>**

### newState()

**<code>newState(options: [Murmur3Opts](/api/interfaces/murmur3opts)): MurmurHash3&lt;&gt;</code>**

## Inherited Methods

### From [ChecksumEncoder](/api/classes/checksumencoder)

`checksumForString()`, `checksumForValue()`, `convertArray()`, `convertBoolean()`, `convertDate()`, `convertDictionary()`, `convertEncodable()`, `convertGenerator()`, `convertKey()`, `convertNull()`, `convertNumber()`, `convertObject()`, `convertSet()`, `convertString()`, `convertUndefined()`, `convertValue()`, `getDefaultOpts()`, `getOutput()`, `isNativeValue()`, `newEncoder()`, `set()`, `setPrimitive()`

*See [ChecksumEncoder](/api/classes/checksumencoder) for detailed documentation*

